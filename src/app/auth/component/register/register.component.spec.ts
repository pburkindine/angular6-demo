import { RegisterPageComponent } from './register.component';

describe('RegisterPageComponent', () => {
  it('should be tested', () => {
    pending('TODO no time for testing this tonight');
  });
});
